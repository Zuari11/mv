'use client'

export function DashboardSidebar() {
  return (
    <aside className="fixed top-14 right-0 z-40 h-[calc(100vh-3.5rem)] w-16 bg-background border-l-2 border-white">
      {/* Empty sidebar content - cleared as requested */}
    </aside>
  )
}
